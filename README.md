# COPILOT-AGENT-NODE

---

A series of GH copilot SDK agents.

double-agent-server.ts contains the most recent agent I built, uses gpt-4.1 for extremely quick scraping and parsing (attaches to running instance of browser using custom chrom extension and mcp server) and gpt-5-mini for more robust reasoning. Express server allows for remote control.
