/* await step(
      "Take quiz",
      'Find the module 2 knowledge check canvas tab and click the take quiz again button'
    );

    await step(
      "Answer the questions",
      'Read the question text and click the answer, click the next button when finished and repeat the process'
    ); */
    await step(
      "Answer the questions",
      'Find the module 2 knowledge check canvas tab and click the correct answer'
    );

    /* await step(
      "sign in",
      'Find and click the "Sign In" or "Login" button.'
    );

    await step(
      "google auth",
      'Find and click the "Sign in with Google" button.'
    ); */